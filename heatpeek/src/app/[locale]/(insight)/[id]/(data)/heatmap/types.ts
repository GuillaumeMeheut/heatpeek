export enum HeatmapType {
  Clicks = "clicks",
  RageClicks = "rage_clicks",
  ScrollDepth = "scroll_depth",
}

export enum DeviceEnum {
  Desktop = "desktop",
  Tablet = "tablet",
  Mobile = "mobile",
}
